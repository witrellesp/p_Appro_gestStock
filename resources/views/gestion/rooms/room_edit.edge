@component('components/layouts/app')
@slot('main')


    <h1>Edit {{room.name}}</h1>
    <form method="POST" action="/room/{{ room.id }}/submit_edit?_method=PUT">
    {{ csrfField() }}
    


      <div>
        <label for="room_name">Nom: </label>
        <input id="room_name" type="text" name="room_name" value="{{room.name}}"/>
      </div>

      <button type="submit">Enregistrer</button>
    </form>

@endslot
@endcomponent